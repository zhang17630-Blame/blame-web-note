# 1. 忽略依赖包文件夹 (千万别传，几百兆起步！)
node_modules/
pnpm-lock.yaml
package-lock.json
yarn.lock

# 2. 忽略环境变量文件 (防止泄露密码)
.env
.env.local
.env.development.local
.env.production.local
.env.test.local

# 3. 忽略构建产物 (Vite 打包生成的，部署时自动生成，不用传)
dist/
dist-ssr/
*.local

# 4. 忽略日志和调试文件
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# 5. 缓存文件
.cache/
.eslintcache
.stylelintcache
vite.svg

# 6. 编辑器和 IDE 配置 (每个人的配置不同，不需要统一)
# 如果你用 VS Code
.vscode/
# 如果你用 WebStorm / IntelliJ
.idea/
# 其他编辑器
*.suo
*.ntvs*
*.njsproj
*.sln

# 7. 系统文件 (Mac 和 Windows 自动生成的)
.DS_Store
Thumbs.db